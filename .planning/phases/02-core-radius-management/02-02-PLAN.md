---
phase: 02-core-radius-management
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/lib/radius-api.ts
  - frontend/src/types/radius.ts
  - frontend/src/app/(dashboard)/users/page.tsx
  - frontend/src/app/(dashboard)/users/new/page.tsx
  - frontend/src/app/(dashboard)/users/[username]/page.tsx
  - frontend/src/app/(dashboard)/users/[username]/edit/page.tsx
  - frontend/src/app/(dashboard)/groups/page.tsx
  - frontend/src/app/(dashboard)/groups/new/page.tsx
  - frontend/src/app/(dashboard)/groups/[groupname]/page.tsx
  - frontend/src/app/(dashboard)/nas/page.tsx
  - frontend/src/app/(dashboard)/nas/new/page.tsx
  - frontend/src/app/(dashboard)/nas/[id]/page.tsx
  - frontend/src/components/radius/UserForm.tsx
  - frontend/src/components/radius/GroupForm.tsx
  - frontend/src/components/radius/NasForm.tsx
  - frontend/src/components/radius/AttributeEditor.tsx
  - frontend/src/components/radius/DataTable.tsx
  - frontend/src/components/layout/sidebar.tsx
autonomous: true
requirements:
  - USER-01
  - USER-02
  - USER-03
  - USER-04
  - USER-07
  - GRP-01
  - GRP-02
  - GRP-03
  - GRP-04
  - NAS-01
  - NAS-02
  - NAS-03
  - NAS-04
  - UX-01
  - UX-02
  - UX-04

must_haves:
  truths:
    - "User navigates to /users and sees a searchable paginated list of RADIUS users in French labels"
    - "User clicks 'Ajouter un utilisateur' and completes a wizard: username, password, then optional reply attributes"
    - "User clicks disable toggle on a user row — user becomes disabled without page reload (optimistic update)"
    - "User navigates to /groups, creates a group with check+reply attributes, assigns members via dropdown"
    - "User navigates to /nas, adds a NAS device, sees a confirmation dialog warning about FreeRADIUS restart"
    - "NAS shared secret is masked (***) in list and detail views; a 'Voir' button reveals it"
    - "All forms show inline validation errors in French before submitting"
    - "Interface renders correctly on 768px wide screen (tablet)"
  artifacts:
    - path: "frontend/src/types/radius.ts"
      provides: "TypeScript interfaces for all RADIUS entities: RadUser, RadGroup, Nas, RadCheck, RadReply, PaginatedResponse"
      exports: ["RadUser", "RadGroup", "Nas", "RadCheckAttr", "RadReplyAttr", "PaginatedResponse"]
    - path: "frontend/src/lib/radius-api.ts"
      provides: "Typed API client functions for all RADIUS endpoints using apiFetch()"
      exports: ["listUsers", "createUser", "updateUser", "deleteUser", "disableUser", "enableUser", "listGroups", "createGroup", "assignUserToGroup", "listNas", "createNas", "getNasSecret"]
    - path: "frontend/src/components/radius/AttributeEditor.tsx"
      provides: "Reusable component for editing radcheck/radreply attribute rows with operator dropdown"
    - path: "frontend/src/components/radius/DataTable.tsx"
      provides: "Generic paginated data table with search input, column headers, and row actions"
  key_links:
    - from: "frontend/src/app/(dashboard)/users/page.tsx"
      to: "frontend/src/lib/radius-api.ts"
      via: "TanStack Query useQuery calling listUsers()"
      pattern: "useQuery.*listUsers"
    - from: "frontend/src/components/radius/UserForm.tsx"
      to: "frontend/src/components/radius/AttributeEditor.tsx"
      via: "AttributeEditor rendered for check_attrs and reply_attrs sections"
      pattern: "AttributeEditor"
    - from: "frontend/src/app/(dashboard)/nas/new/page.tsx"
      to: "restart confirmation dialog"
      via: "AlertDialog from shadcn/ui shown before calling createNas()"
      pattern: "AlertDialog"
---

<objective>
Build all three management sections of the frontend: RADIUS Users, Groups, and NAS devices.

Purpose: This is the primary user-facing deliverable of Phase 2 — the UI that network administrators will use daily. Depends on plan 02-01 API endpoints.

Output:
- TypeScript types + API client (contracts first)
- 3 management sections: /users, /groups, /nas (list + create + detail/edit pages)
- Shared components: DataTable, AttributeEditor, form components
- French labels throughout (per UX-04)
- Task-oriented wizard flows (per UX-01)
- NAS restart confirmation dialog (per NAS-04, CP-3)
</objective>

<execution_context>
@E:/Documents/NXO/PROJETS/radius-ui/.claude/get-shit-done/workflows/execute-plan.md
@E:/Documents/NXO/PROJETS/radius-ui/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/02-core-radius-management/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Phase 1 that this plan builds on. Do NOT recreate. -->

From frontend/src/lib/api.ts:
```typescript
// Use apiFetch for ALL API calls — it handles auth headers + auto-refresh
export async function apiFetch<T>(
  path: string,
  options?: RequestInit & { params?: Record<string, string | number> }
): Promise<T>
```

From frontend/src/lib/auth.tsx:
```typescript
export function useAuth(): {
  user: { email: string; role: string } | null;
  logout: () => void;
  isLoading: boolean;
}
```

From Phase 1 frontend structure:
- Layout: frontend/src/app/(dashboard)/layout.tsx — wraps all dashboard pages with Sidebar + Header
- Sidebar: frontend/src/components/layout/sidebar.tsx — has placeholder nav links, MUST be updated to add Users/Groups/NAS links
- shadcn/ui components available: Button, Input, Card, Label (installed in Phase 1)
- TanStack Query v5 available: QueryClientProvider already in providers.tsx
- Tailwind CSS 4 (CSS-first, no tailwind.config.js) — use utility classes as normal

From 02-01 API (contracts to consume):
```
GET    /api/v1/radius/users?search=&page=1&page_size=25  → PaginatedResponse<UserResponse>
POST   /api/v1/radius/users                               → UserResponse 201
GET    /api/v1/radius/users/{username}                    → UserResponse
PUT    /api/v1/radius/users/{username}                    → UserResponse
DELETE /api/v1/radius/users/{username}                    → 204
POST   /api/v1/radius/users/{username}/disable            → UserResponse
POST   /api/v1/radius/users/{username}/enable             → UserResponse
GET    /api/v1/radius/groups?search=&page=1&page_size=25  → PaginatedResponse<GroupResponse>
POST   /api/v1/radius/groups                              → GroupResponse 201
GET    /api/v1/radius/groups/{groupname}                  → GroupResponse
PUT    /api/v1/radius/groups/{groupname}                  → GroupResponse
DELETE /api/v1/radius/groups/{groupname}                  → 204
POST   /api/v1/radius/groups/{groupname}/members          → RadUserGroupOut 201
GET    /api/v1/nas?search=&page=1&page_size=25            → PaginatedResponse<NasResponse> (secrets masked)
POST   /api/v1/nas                                        → NasResponse + { restart_triggered: boolean } 201
PUT    /api/v1/nas/{id}                                   → NasResponse + { restart_triggered: boolean }
DELETE /api/v1/nas/{id}                                   → 204 + restarts FreeRADIUS
GET    /api/v1/nas/{id}/secret                            → { secret: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types + API client + shared components</name>
  <files>
    frontend/src/types/radius.ts
    frontend/src/lib/radius-api.ts
    frontend/src/components/radius/DataTable.tsx
    frontend/src/components/radius/AttributeEditor.tsx
  </files>
  <read_first>
    - frontend/src/lib/api.ts (apiFetch signature — use this for all calls)
    - frontend/src/lib/auth.tsx (useAuth hook)
    - frontend/src/app/(dashboard)/page.tsx (existing page pattern to follow for data fetching)
    - frontend/src/components/layout/sidebar.tsx (to understand existing component style)
  </read_first>
  <action>
    CREATE frontend/src/types/radius.ts — TypeScript interfaces that exactly mirror the 02-01 API schemas:

    ```typescript
    export interface RadCheckAttr {
      id: number;
      username: string;
      attribute: string;
      op: string;
      value: string;
    }

    export interface RadReplyAttr {
      id: number;
      username: string;
      attribute: string;
      op: string;
      value: string;
    }

    export interface RadUser {
      username: string;
      check_attrs: RadCheckAttr[];
      reply_attrs: RadReplyAttr[];
      groups: string[];
      disabled: boolean;
    }

    export interface RadGroupCheckAttr {
      id: number;
      groupname: string;
      attribute: string;
      op: string;
      value: string;
    }

    export interface RadGroup {
      groupname: string;
      check_attrs: RadGroupCheckAttr[];
      reply_attrs: RadGroupReplyAttr[];
      members: RadUserGroupMember[];
    }

    export interface RadUserGroupMember {
      username: string;
      groupname: string;
      priority: number;
    }

    export interface Nas {
      id: number;
      nasname: string;
      shortname: string | null;
      type: string;
      ports: number | null;
      secret_masked: string;
      server: string | null;
      community: string | null;
      description: string | null;
    }

    export interface NasWithSecret extends Nas {
      secret: string;
    }

    export interface PaginatedResponse<T> {
      items: T[];
      total: number;
      page: number;
      page_size: number;
    }

    export interface NasMutationResponse extends Nas {
      restart_triggered: boolean;
    }
    ```

    CREATE frontend/src/lib/radius-api.ts — typed API client:
    - Import apiFetch from "@/lib/api"
    - One function per endpoint, all typed with generics
    - listUsers(params: { search?: string; page?: number; page_size?: number }): Promise<PaginatedResponse<RadUser>>
    - createUser(data: { username: string; password: string; reply_attrs?: Omit<RadReplyAttr, "id"|"username">[] }): Promise<RadUser>
    - updateUser, deleteUser, disableUser, enableUser follow same pattern
    - listGroups, createGroup, updateGroup, deleteGroup, assignUserToGroup, removeUserFromGroup
    - listNas, createNas(NasCreate), updateNas, deleteNas, getNasSecret(id): Promise<{secret: string}>

    CREATE frontend/src/components/radius/DataTable.tsx — generic reusable table:
    - Props: columns: ColumnDef[], data: T[], total: number, page: number, pageSize: number, onPageChange, onSearch, searchPlaceholder, isLoading, actions?: React.ReactNode
    - Renders: search input at top, shadcn/ui Table with column headers, pagination controls at bottom (Précédent / Suivant with page indicator "Page X sur Y")
    - Empty state: "Aucun résultat trouvé" with icon
    - Loading state: skeleton rows (3 rows of shimmer divs)
    - Use shadcn/ui Table component — install via `npx shadcn@latest add table` if not present
    - All text in French: "Rechercher...", "Précédent", "Suivant", "Chargement..."

    CREATE frontend/src/components/radius/AttributeEditor.tsx — attribute row editor:
    - Props: attrs: AttrRow[], onChange: (attrs: AttrRow[]) => void, context: "check" | "reply"
    - AttrRow: { attribute: string; op: string; value: string }
    - Renders a list of rows, each with: attribute Input, op Select dropdown, value Input, delete Button
    - op Select options: context=="check" → [":=", "==", "+=", "!="] with French tooltips; context=="reply" → [":=", "=", "+="]
    - Default op pre-selected: ":=" for both contexts
    - "Ajouter un attribut" button appends blank row
    - Validation: if attribute contains "Password" (case-insensitive) and context=="check", force op=":=" and disable the Select (greyed out with tooltip "Requis pour les mots de passe")
    - Use shadcn/ui Select component — install via `npx shadcn@latest add select` if not present
  </action>
  <verify>
    <automated>cd E:/Documents/NXO/PROJETS/radius-ui/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with 0 errors on the new files
    - `grep -c "export" frontend/src/types/radius.ts` returns >= 8 (all entity types exported)
    - `grep -c "export" frontend/src/lib/radius-api.ts` returns >= 12 (all API functions exported)
    - `grep "Précédent\|Suivant\|Aucun résultat\|Rechercher" frontend/src/components/radius/DataTable.tsx | wc -l` returns >= 4 (French labels present)
    - `grep "Password.*op.*:=" frontend/src/components/radius/AttributeEditor.tsx` returns >= 1 line (password op enforcement present)
  </acceptance_criteria>
  <done>TypeScript contracts defined. API client covers all 02-01 endpoints. DataTable and AttributeEditor components usable by all three management sections.</done>
</task>

<task type="auto">
  <name>Task 2: Users, Groups, NAS pages with task-oriented workflows</name>
  <files>
    frontend/src/app/(dashboard)/users/page.tsx
    frontend/src/app/(dashboard)/users/new/page.tsx
    frontend/src/app/(dashboard)/users/[username]/page.tsx
    frontend/src/app/(dashboard)/users/[username]/edit/page.tsx
    frontend/src/app/(dashboard)/groups/page.tsx
    frontend/src/app/(dashboard)/groups/new/page.tsx
    frontend/src/app/(dashboard)/groups/[groupname]/page.tsx
    frontend/src/app/(dashboard)/nas/page.tsx
    frontend/src/app/(dashboard)/nas/new/page.tsx
    frontend/src/app/(dashboard)/nas/[id]/page.tsx
    frontend/src/components/radius/UserForm.tsx
    frontend/src/components/radius/GroupForm.tsx
    frontend/src/components/radius/NasForm.tsx
    frontend/src/components/layout/sidebar.tsx
  </files>
  <read_first>
    - frontend/src/components/radius/DataTable.tsx (after Task 1 — use in list pages)
    - frontend/src/components/radius/AttributeEditor.tsx (after Task 1 — use in forms)
    - frontend/src/lib/radius-api.ts (after Task 1 — API functions)
    - frontend/src/types/radius.ts (after Task 1 — type imports)
    - frontend/src/app/(dashboard)/layout.tsx (page wrapper — all pages are rendered inside this)
    - frontend/src/components/layout/sidebar.tsx (add nav links for Users, Groups, NAS)
  </read_first>
  <action>
    All pages are Client Components ("use client") using TanStack Query v5 for data fetching.

    SIDEBAR UPDATE (frontend/src/components/layout/sidebar.tsx):
    Add 3 nav items with lucide-react icons and French labels:
    - /users — icon: Users — label: "Utilisateurs"
    - /groups — icon: Users2 — label: "Groupes"
    - /nas — icon: Server — label: "Équipements NAS"
    Active state: highlight current path using usePathname() from next/navigation.

    USERS SECTION:

    /users/page.tsx — list page:
    - useQuery calling listUsers({ search, page, page_size: 25 })
    - Renders DataTable with columns: Nom d'utilisateur, Groupes, Statut (Actif/Désactivé badge), Actions
    - Status badge: green "Actif" / red "Désactivé" using shadcn/ui Badge
    - Actions per row: Modifier button, Désactiver/Activer toggle button (useMutation + optimistic update), Supprimer button with AlertDialog confirmation
    - Header: title "Utilisateurs RADIUS" + "Ajouter un utilisateur" button (→ /users/new)

    /users/new/page.tsx — creation wizard:
    - Step 1: username (text input, required, validate no spaces) + password (password input, required, min 8 chars)
    - Step 2 (optional): Reply attributes using AttributeEditor (context="reply") — pre-loaded with common examples as greyed-out placeholders: "Framed-IP-Address", "Tunnel-Type", "Tunnel-Medium-Type", "Tunnel-Private-Group-Id"
    - Submit: calls createUser() via useMutation; on success navigate to /users/{username}; on error show toast "Erreur lors de la création"
    - Labels: "Nom d'utilisateur", "Mot de passe", "Attributs de réponse (optionnel)", "Créer l'utilisateur", "Annuler"

    /users/[username]/page.tsx — detail view:
    - Shows user info: username, disabled status badge, groups list
    - Two tabs (shadcn/ui Tabs): "Attributs" tab shows check_attrs and reply_attrs in read-only tables; "Historique" tab is a placeholder with text "Disponible en Phase 3"
    - Actions: "Modifier" → /users/[username]/edit; disable/enable toggle; delete with confirmation

    /users/[username]/edit/page.tsx — edit form:
    - Renders UserForm.tsx pre-populated with current user data
    - UserForm.tsx: username (read-only input), AttributeEditor for check_attrs (context="check"), AttributeEditor for reply_attrs (context="reply"), Save/Cancel buttons

    GROUPS SECTION:

    /groups/page.tsx — list page:
    - DataTable with columns: Nom du groupe, Nb d'attributs, Nb de membres, Actions (Modifier, Supprimer)
    - Header: "Groupes RADIUS" + "Ajouter un groupe" button

    /groups/new/page.tsx — creation:
    - GroupForm.tsx with: groupname input, AttributeEditor check_attrs (context="check"), AttributeEditor reply_attrs (context="reply")
    - Labels: "Nom du groupe", "Attributs de vérification", "Attributs de réponse", "Créer le groupe"

    /groups/[groupname]/page.tsx — detail with members:
    - Two sections: group attributes display + members section
    - Members section: shows list of RadUserGroupMember rows with username + priority; "Ajouter un membre" button opens a Dialog with a username input + priority number input (default 1); "Retirer" button per row

    NAS SECTION:

    /nas/page.tsx — list page:
    - DataTable with columns: IP/Nom, Nom court, Type, Description, Secret (always "***"), Actions (Modifier, Voir le secret, Supprimer)
    - "Voir le secret" action: calls getNasSecret(id) on click, shows result in a Dialog with a copy button; auto-hides after 30s
    - Header: "Équipements NAS" + "Ajouter un équipement"

    /nas/new/page.tsx — creation with restart warning:
    - NasForm.tsx with all fields: IP/Nom (nasname, required), Nom court (shortname), Type (select: other/cisco/motorola/mikrotik/…), Ports, Secret partagé (secret, required), Serveur, Communauté, Description
    - BEFORE submitting: show AlertDialog with text "Ajouter cet équipement NAS nécessite un redémarrage de FreeRADIUS. Souhaitez-vous continuer ?" with buttons "Annuler" and "Redémarrer et ajouter"
    - On confirm: call createNas(); if response.restart_triggered show success toast "NAS ajouté et FreeRADIUS redémarré"; if restart_triggered=false show warning toast "NAS ajouté mais le redémarrage a échoué — redémarrage manuel requis"

    /nas/[id]/page.tsx — detail + edit:
    - Shows NasForm pre-populated (password field shows placeholder "••••••••" — leave blank to keep existing)
    - Update shows same restart AlertDialog
    - Delete button with AlertDialog: same restart warning message

    RESPONSIVE DESIGN for all pages:
    - List pages: DataTable scrollable horizontally on mobile; on tablet (768px) all columns visible
    - Forms: single column on mobile, 2-column grid on tablet+ for multi-field sections
    - Use Tailwind responsive prefixes: `sm:`, `md:` for breakpoints
  </action>
  <verify>
    <automated>cd E:/Documents/NXO/PROJETS/radius-ui/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with 0 errors
    - `find frontend/src/app/\(dashboard\) -name "page.tsx" | wc -l` returns >= 10 (all section pages exist)
    - `grep -r "AlertDialog" frontend/src/app/\(dashboard\)/nas/ | wc -l` returns >= 2 (restart confirmation in NAS create and update)
    - `grep -r "restart_triggered" frontend/src/app/\(dashboard\)/nas/ | wc -l` returns >= 2 (handles restart_triggered response field)
    - `grep "Équipements NAS\|Utilisateurs\|Groupes" frontend/src/components/layout/sidebar.tsx | wc -l` returns >= 3 (nav links added)
    - `grep -r "useClient\|\"use client\"" frontend/src/app/\(dashboard\)/users/page.tsx` returns 1 (client component)
    - `grep "Désactiver\|Activer\|Modifier\|Supprimer\|Ajouter" frontend/src/app/\(dashboard\)/users/page.tsx | wc -l` returns >= 4 (French action labels)
  </acceptance_criteria>
  <done>All 10 management pages exist. French labels throughout. NAS restart AlertDialog implemented. Secret masking in NAS list with reveal dialog. Sidebar updated with 3 nav links. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
TypeScript build:
```bash
cd E:/Documents/NXO/PROJETS/radius-ui/frontend && npx tsc --noEmit
```
Expected: 0 errors.

File existence:
```bash
find E:/Documents/NXO/PROJETS/radius-ui/frontend/src/app/\(dashboard\) -name "page.tsx" | sort
```
Expected: at minimum users, users/new, users/[username], users/[username]/edit, groups, groups/new, groups/[groupname], nas, nas/new, nas/[id].

French label check:
```bash
grep -r "Utilisateurs RADIUS\|Groupes RADIUS\|Équipements NAS" E:/Documents/NXO/PROJETS/radius-ui/frontend/src/app/\(dashboard\)/ | wc -l
```
Expected: >= 3.

Restart dialog check:
```bash
grep -r "AlertDialog\|redémarrage\|restart" E:/Documents/NXO/PROJETS/radius-ui/frontend/src/app/\(dashboard\)/nas/ | wc -l
```
Expected: >= 3.
</verification>

<success_criteria>
1. frontend/src/types/radius.ts exports TypeScript interfaces for all RADIUS entities
2. frontend/src/lib/radius-api.ts provides typed functions for all 17+ API endpoints
3. DataTable component is reusable across all three sections (used by users, groups, nas list pages)
4. AttributeEditor enforces op=":=" for Password attributes in check context (CP-2 prevention in UI)
5. NAS create/edit/delete shows AlertDialog restart warning before submission (NAS-04, CP-3)
6. NAS secrets shown as "***" in all list/detail views; explicit reveal via dialog with 30s auto-hide (NAS-03)
7. Sidebar updated with 3 nav links in French
8. All pages compile with 0 TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-radius-management/02-02-SUMMARY.md` following the template at @E:/Documents/NXO/PROJETS/radius-ui/.claude/get-shit-done/templates/summary.md
</output>
